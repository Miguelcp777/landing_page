// Portfolio editorial: translations and a lightweight, decorative data sculpture.
translations.es.studio = {
    intro: 'MIGUEL CASTILLO · PORTFOLIO PERSONAL', head1: 'Entiendo el negocio.', head2: 'Lo conecto con', head3: 'datos e IA.',
    description: 'Conecto mi experiencia en operaciones con el análisis de negocio: preparo datos, analizo tendencias y construyo dashboards para apoyar decisiones.',
    explore: 'Explorar mis proyectos', about: 'Un poco sobre mí', art: 'IDEAS EN MOVIMIENTO', artCaption: 'IA × AUTOMATIZACIÓN × DATOS',
    pause: 'Pausar animación', resume: 'Activar animación', foot: 'De entender un problema a construir una solución.', portrait: 'Servicio técnico · Iberia',
    background: 'CAPACIDADES EN LA PRÁCTICA', toolboxLabel: '02 / CAPACIDADES Y HERRAMIENTAS', toolboxTitle: 'Mi caja de herramientas.',
    toolboxText: 'De los datos a las decisiones: SQL, Python, Tableau, Power BI y Alteryx. Utilizo IA como apoyo para programar y sigo desarrollando mis capacidades técnicas.',
    aiTitle: 'Inteligencia artificial', aiText: 'Uso modelos de IA para razonar, crear aplicaciones y trabajar con documentos. Exploro agentes y asistentes con contexto.',
    autoTitle: 'Automatización', autoText: 'Conecto herramientas, APIs y pasos repetitivos para construir flujos de trabajo y procesamiento de documentos.',
    analysisTitle: 'Análisis de negocio y datos', analysisText: 'Realizo análisis descriptivos y exploratorios, comparo tendencias y segmentos y construyo KPIs y dashboards para entender el rendimiento operativo y apoyar decisiones de negocio.',
    engineeringTitle: 'Preparación y transformación de datos', engineeringText: 'Limpieza, transformación y data wrangling: integro fuentes, trato duplicados y valores nulos y creo variables, métricas y conjuntos de datos derivados para el análisis.',
    toolsUsed: 'Herramientas utilizadas:', skip: 'Saltar al contenido', name: 'Nombre', email: 'Email', message: 'Mensaje', cv: 'Ver mi CV ↗'
};
translations.en.studio = {
    intro: 'MIGUEL CASTILLO · PERSONAL PORTFOLIO', head1: 'Understanding business.', head2: 'Connecting it with', head3: 'data and AI.',
    description: 'I connect my operations experience with business analysis: preparing data, analysing trends and building dashboards to support decisions.',
    explore: 'Explore my projects', about: 'A little about me', art: 'IDEAS IN MOTION', artCaption: 'AI × AUTOMATION × DATA',
    pause: 'Pause animation', resume: 'Start animation', foot: 'From understanding a problem to building a solution.', portrait: 'Technical service · Iberia',
    background: 'CAPABILITIES IN PRACTICE', toolboxLabel: '02 / CAPABILITIES & TOOLS', toolboxTitle: 'My toolbox.',
    toolboxText: 'From data to decisions: SQL, Python, Tableau, Power BI and Alteryx. I use AI support for coding while continuing to develop my technical skills.',
    aiTitle: 'Artificial intelligence', aiText: 'I use AI models to reason, build applications and work with documents. I explore agents and assistants with context.',
    autoTitle: 'Automation', autoText: 'I connect tools, APIs and repetitive steps to build workflows and document processing tools.',
    analysisTitle: 'Business & data analysis', analysisText: 'I carry out descriptive and exploratory analysis, compare trends and segments, and build KPIs and dashboards to understand operational performance and support business decisions.',
    engineeringTitle: 'Data preparation & transformation', engineeringText: 'Data cleaning, transformation and wrangling: combining sources, handling duplicates and missing values, and creating derived variables, metrics and datasets for analysis.',
    toolsUsed: 'Tools used:', skip: 'Skip to content', name: 'Name', email: 'Email', message: 'Message', cv: 'View my CV ↗'
};
translations.es.studio.conceptArt = 'Ilustración IA';
translations.es.studio.syntheticUi = 'Interfaz real · Cifras difuminadas';
translations.en.studio.syntheticUi = 'Live interface · Figures obscured';
// The planner cover is a drawing, not a capture, and its figures are shown
// rather than blurred. Saying otherwise would be a small, pointless lie.
translations.es.studio.chartCaption = 'Ilustración · cifras sintéticas';
translations.en.studio.chartCaption = 'Illustration · synthetic figures';
translations.en.studio.conceptArt = 'AI illustration';
Object.assign(translations.es.studio, { orbitMode: 'Órbita', neuralMode: 'Red neuronal' });
Object.assign(translations.en.studio, { orbitMode: 'Orbit', neuralMode: 'Neural network' });
Object.assign(translations.es.studio, { bioLabel: 'PERSONAS · PROCESOS · TECNOLOGÍA', bioStatement: 'Conectar operaciones, datos y decisiones.', journeyLabel: 'MI TRAYECTORIA', journeyIntro: 'De resolver problemas técnicos a coordinar equipos y conectar operaciones con tecnología.' });
Object.assign(translations.en.studio, { bioLabel: 'PEOPLE · PROCESSES · TECHNOLOGY', bioStatement: 'Connecting operations, data and decisions.', journeyLabel: 'MY JOURNEY', journeyIntro: 'From solving technical problems to coordinating teams and connecting operations with technology.' });
Object.assign(translations.es.studio, { toolboxArtwork: 'Ilustraciones conceptuales con IA', pauseToolbox: 'Pausar movimiento', resumeToolbox: 'Activar movimiento' });
Object.assign(translations.en.studio, { toolboxArtwork: 'AI concept illustrations', pauseToolbox: 'Pause motion', resumeToolbox: 'Start motion' });
Object.assign(translations.es.nav, { data: 'Herramientas', projects: 'Proyectos' });
Object.assign(translations.en.nav, { data: 'Toolbox', projects: 'Projects' });
Object.assign(translations.es.about, {
    title: 'La persona detrás de los proyectos.',
    p1: 'Soy Miguel. Coordino el servicio técnico de Johnson & Johnson Vision en España y Portugal, trabajando con equipos y personas en el ámbito de la oftalmología.',
    p2: 'Mi enfoque combina conocimiento de operaciones y análisis de datos: convertir preguntas de negocio en análisis, preparar y validar información y comunicar resultados con claridad. Trabajo con limpieza y transformación de datos, análisis descriptivo y visualización, con apoyo de IA para desarrollar código.'
});
Object.assign(translations.en.about, {
    title: 'The person behind the projects.',
    p1: 'I’m Miguel. I coordinate technical service at Johnson & Johnson Vision in Spain and Portugal, working with people and equipment in ophthalmology.',
    p2: 'My approach combines operations knowledge and data analysis: turning business questions into analysis, preparing and validating information, and communicating findings clearly. I work with data cleaning and transformation, descriptive analysis and visualisation, with AI support for coding.'
});
Object.assign(translations.es.projects, { title: 'Ideas que toman forma.', subtitle: 'Una selección de lo que construyo: agentes de IA, herramientas de gestión y aplicaciones. Abre cada proyecto para conocer su propósito y su tecnología.' });
Object.assign(translations.en.projects, { title: 'Ideas taking shape.', subtitle: 'A selection of what I build: AI agents, management tools and applications. Open each project to explore its purpose and technology.' });
Object.assign(translations.es.data, { title: 'Los datos también cuentan historias.', subtitle: 'Análisis e integración aplicados a operaciones. Explora cada caso para conocer el proyecto y su esquema conceptual.', preview: 'Datos sintéticos · Cifras difuminadas' });
Object.assign(translations.en.data, { title: 'Data tells stories, too.', subtitle: 'Analytics and integration applied to operations. Explore each case to discover the project and its concept diagram.', preview: 'Synthetic data · Figures obscured' });
Object.assign(translations.es.contact, { title: 'Sigamos conectados.', text: 'Puedes encontrarme en LinkedIn o escribirme para conversar sobre tecnología, datos o alguno de mis proyectos.', scan_qr: 'Escanea para guardar mi contacto' });
Object.assign(translations.en.contact, { title: 'Let’s stay connected.', text: 'Find me on LinkedIn or write to me to talk about technology, data or one of my projects.', scan_qr: 'Scan to save my contact' });

// Editorial profile: bilingual content, without decorative animation handlers.
const editorialCopy = {
 en: {
  meta: {title: 'Miguel Castillo | Data Analyst · SQL, Python & BI', description: 'Data analyst working with SQL, Python, Tableau and Power BI, with nine years of medtech service operations behind the questions. Analytical cases and applications built.'},
  nav: {cases: 'Analytical cases', data: 'Capabilities', projects: 'Projects'},
  studio: {intro: 'DATA ANALYST · BUSINESS INTELLIGENCE · SERVICE OPERATIONS', head1: 'Turning operational data into', head2: 'business insight.', description: 'I combine technical service experience with data preparation, KPIs and dashboards to understand operations and support business decisions.', explore: 'Explore analytical cases', cv: 'View CV', portraitRole: 'Technical Service Supervisor, Iberia', portraitOrg: 'Johnson & Johnson Vision', toolboxLabel: '02 / CAPABILITIES', toolboxTitle: 'From raw data to clear decisions.', method: 'My approach: understand the question, prepare the data, analyse and validate, then communicate the findings. I use AI assistance for coding and review the outputs against source data and business rules.', bioLabel: 'OPERATIONS · PEOPLE · DATA', bioStatement: 'I don’t analyse service operations from the outside. I run them.', journeyIntro: 'Nine years from hands-on service engineering to building the reporting that EMEA service decisions run on.'},
  about: {title: 'The experience behind the analysis.', p1: 'I coordinate technical service at Johnson & Johnson Vision in Spain and Portugal, working with teams and ophthalmic equipment.', p2: 'This operational perspective shapes the questions I bring to data: how to understand service costs, follow contracts and make information useful for planning. My work combines data preparation, descriptive analysis and visualisation.'},
  data: {label: '01 / SELECTED ANALYTICAL CASES', title: 'Questions, data, decisions.', subtitle: 'Three examples of analysis applied to service operations. Explore the business question, the data work and the decision each case supports.', preview: 'Synthetic data · Figures obscured'},
  projects: {title: 'Building beyond the analysis.', subtitle: 'Complementary work in planning tools and document automation.', more: 'More applications and personal projects'},
  contact: {title: 'Let’s talk about business and data.', text: 'For opportunities in data analysis and business intelligence, contact me by email or LinkedIn, or view my CV.'},
  skills: {prepare: {title: 'Data preparation & quality', text: 'Clean and transform data, combine sources, handle missing values and duplicates, and create derived variables and datasets.'}, analyse: {title: 'Descriptive & exploratory analysis', text: 'Compare trends and segments, calculate metrics and examine data quality to answer operational questions.'}, communicate: {title: 'Visualisation & communication', text: 'Build KPIs, dashboards and clear summaries that connect analytical findings with business decisions.'}},
  cases: {work: 'Data work', value: 'What it changed', open: 'Explore the case ↗', contribution: 'My contribution', limitations: 'Why there is no live data',
   dash4: {title: 'Install Base — single source of truth', question: 'How do you turn equipment and contract records held in separate enterprise systems into one source the service organisation can plan from?', work: 'Consolidate the sources in Alteryx, reconcile records against each other and check coverage and data quality before anything reaches a dashboard.', value: 'One install base view for EMEA showing what is installed, where, and which contract covers it — and surfacing records that disagree between systems.', contribution: 'I built the Alteryx pipeline that merges three enterprise systems into a single reconciled record per asset, and the Tableau dashboard on top of it, covering the EMEA install base by cluster, country, platform and customer — 1,000+ assets across 50+ countries. It doubles as the data-quality instrument: it makes visible when equipment records are incomplete or disagree between systems, and shows where the base is growing or shrinking by platform.', limitations: 'Every figure shown is synthetic and deliberately blurred. The production dashboards run on customer and contract records I cannot publish, so what you see reproduces the structure and the method, never the numbers.'},
   dash1: {title: 'Cost per System', question: 'Where does service cost actually sit, once you can compare it by equipment, service type and period?', work: 'Prepare cost data in Alteryx, then compare labour, travel and spare-part cost across platform, country, cluster and contract type.', value: 'A cost-per-system view for EMEA showing which platforms and which service types drive cost, and how that moves year over year.', contribution: 'I built the Alteryx ETL and the Tableau dashboard, aggregating cost per system across cluster, country, platform, customer and contract type. Alongside it I built the Salesforce reporting for the service KPIs behind those costs: PM completion, calls per system, callback rate and work-order completion.', limitations: 'Every figure shown is synthetic and deliberately blurred. The production dashboards run on customer and contract records I cannot publish, so what you see reproduces the structure and the method, never the numbers.'},
   dash2: {title: 'Contract Profitability — ASP', question: 'Which maintenance contracts are actually profitable, and on which platforms and for which customers?', work: 'Bring contract, revenue and cost together into comparable metrics, including average sales price per contract type.', value: 'The ASP analysis is used to reprice maintenance contracts: it shows which contract types return least against their cost, by platform and by customer. A companion dashboard flags contracts approaching renewal for the sales team.', contribution: 'I built the ASP and revenue dashboards in Tableau on the same Alteryx pipeline, and the contract-expiry view the sales team works from. The output feeds pricing decisions rather than only reporting them.', limitations: 'Every figure shown is synthetic and deliberately blurred. The production dashboards run on customer and contract records I cannot publish, so what you see reproduces the structure and the method, never the numbers.'}
  }
 },
 es: {
  meta: {title: 'Miguel Castillo | Data Analyst · SQL, Python y BI', description: 'Analista de datos con SQL, Python, Tableau y Power BI, y nueve años de operaciones de servicio medtech detrás de las preguntas. Casos de análisis y aplicaciones construidas.'},
  nav: {cases: 'Casos de análisis', data: 'Capacidades', projects: 'Proyectos'},
  studio: {intro: 'DATA ANALYST · BUSINESS INTELLIGENCE · SERVICE OPERATIONS', head1: 'De los datos operativos al', head2: 'conocimiento del negocio.', description: 'Combino mi experiencia en servicio técnico con preparación de datos, KPIs y dashboards para entender las operaciones y apoyar decisiones de negocio.', explore: 'Explorar casos de análisis', cv: 'Ver CV', portraitRole: 'Supervisor de Servicio T\u00e9cnico, Iberia', portraitOrg: 'Johnson & Johnson Vision', toolboxLabel: '02 / CAPACIDADES', toolboxTitle: 'De los datos a decisiones claras.', method: 'Mi enfoque: entender la pregunta, preparar los datos, analizar y validar, y comunicar los hallazgos. Utilizo IA como apoyo para programar y reviso los resultados frente a los datos de origen y las reglas de negocio.', bioLabel: 'OPERACIONES · PERSONAS · DATOS', bioStatement: 'No analizo las operaciones de servicio desde fuera. Las dirijo.', journeyIntro: 'Nueve años desde el servicio técnico de campo hasta construir el reporting sobre el que se deciden las operaciones de servicio en EMEA.'},
  about: {title: 'La experiencia detrás del análisis.', p1: 'Coordino el servicio técnico de Johnson & Johnson Vision en España y Portugal, trabajando con equipos y personas en el ámbito de la oftalmología.', p2: 'Esa perspectiva operativa guía las preguntas que planteo a los datos: entender los costes del servicio, seguir los contratos y facilitar la planificación. Mi trabajo combina preparación de datos, análisis descriptivo y visualización.'},
  data: {label: '01 / CASOS DE ANÁLISIS', title: 'Preguntas, datos, decisiones.', subtitle: 'Tres ejemplos de análisis aplicado a operaciones de servicio. Explora la pregunta de negocio, el trabajo con los datos y la decisión que apoya cada caso.', preview: 'Datos sintéticos · Cifras difuminadas'},
  projects: {title: 'Más allá del análisis.', subtitle: 'Proyectos complementarios de planificación y automatización documental.', more: 'Más aplicaciones y proyectos personales'},
  contact: {title: 'Hablemos de negocio y datos.', text: 'Para oportunidades en análisis de datos y business intelligence, escríbeme por email o LinkedIn, o consulta mi CV.'},
  skills: {prepare: {title: 'Preparación y calidad de datos', text: 'Limpieza, transformación y data wrangling: combinar fuentes, tratar valores nulos y duplicados y crear variables y conjuntos de datos derivados.'}, analyse: {title: 'Análisis descriptivo y exploratorio', text: 'Comparar tendencias y segmentos, calcular métricas y examinar la calidad de los datos para responder a preguntas operativas.'}, communicate: {title: 'Visualización y comunicación', text: 'Construir KPIs, dashboards y resúmenes claros que conecten los hallazgos del análisis con decisiones de negocio.'}},
  cases: {work: 'Trabajo con los datos', value: 'Qué cambió', open: 'Explorar el caso ↗', contribution: 'Mi contribución', limitations: 'Por qué no hay datos reales',
   dash4: {title: 'Base instalada — fuente única de verdad', question: '¿Cómo convertir los registros de equipos y contratos repartidos en varios sistemas corporativos en una fuente única desde la que planificar el servicio?', work: 'Consolidar las fuentes en Alteryx, conciliar los registros entre sí y comprobar cobertura y calidad antes de que nada llegue a un dashboard.', value: 'Una vista única de base instalada en EMEA: qué hay instalado, dónde y bajo qué contrato, sacando a la luz los registros que no cuadran entre sistemas.', contribution: 'Construí el pipeline en Alteryx que fusiona tres sistemas corporativos en un único registro conciliado por activo, y el dashboard de Tableau sobre él, cubriendo la base instalada de EMEA por cluster, país, plataforma y cliente — más de 1.000 activos en más de 50 países. Además es el instrumento de calidad de dato: hace visible cuándo un equipo tiene registros incompletos o contradictorios entre sistemas, y dónde crece o decrece la base por plataforma.', limitations: 'Todas las cifras que se ven son sintéticas y están difuminadas a propósito. Los dashboards en producción corren sobre registros de clientes y contratos que no puedo publicar, así que lo que se muestra reproduce la estructura y el método, nunca los números.'},
   dash1: {title: 'Coste por sistema', question: '¿Dónde está realmente el coste de servicio, cuando puedes compararlo por equipo, tipo de servicio y periodo?', work: 'Preparar los datos de coste en Alteryx y comparar mano de obra, desplazamiento y repuestos por plataforma, país, cluster y tipo de contrato.', value: 'Una vista de coste por sistema en EMEA que muestra qué plataformas y qué tipos de servicio empujan el coste, y cómo se mueve de un año a otro.', contribution: 'Construí el ETL en Alteryx y el dashboard de Tableau, agregando el coste por sistema por cluster, país, plataforma, cliente y tipo de contrato. En paralelo construí el reporting en Salesforce de los KPIs de servicio que hay detrás de esos costes: PM completion, llamadas por sistema, tasa de callback y cierre de órdenes de trabajo.', limitations: 'Todas las cifras que se ven son sintéticas y están difuminadas a propósito. Los dashboards en producción corren sobre registros de clientes y contratos que no puedo publicar, así que lo que se muestra reproduce la estructura y el método, nunca los números.'},
   dash2: {title: 'Rentabilidad de contratos — ASP', question: '¿Qué contratos de mantenimiento son realmente rentables, y en qué plataformas y con qué clientes?', work: 'Combinar contratos, ingresos y costes en métricas comparables, incluido el precio medio de venta por tipo de contrato.', value: 'El análisis de ASP se usa para reajustar el precio de los contratos: muestra qué tipos rinden menos frente a su coste, por plataforma y por cliente. Otro dashboard avisa al equipo de ventas de los contratos próximos a vencer.', contribution: 'Construí los dashboards de ASP e ingresos en Tableau sobre el mismo pipeline de Alteryx, y la vista de vencimientos con la que trabaja el equipo de ventas. El resultado alimenta decisiones de precio, no solo las reporta.', limitations: 'Todas las cifras que se ven son sintéticas y están difuminadas a propósito. Los dashboards en producción corren sobre registros de clientes y contratos que no puedo publicar, así que lo que se muestra reproduce la estructura y el método, nunca los números.'}
  }
 }
};
for (const lang of ['en', 'es']) {
    const copy = editorialCopy[lang];
    for (const [section, values] of Object.entries(copy)) {
        translations[lang][section] = Object.assign(translations[lang][section] || {}, values);
    }
    for (const id of ['dash4', 'dash1', 'dash2']) {
        const c = copy.cases[id];
        translations[lang].projects[id] = { title: c.title, desc: `<p>${c.question}</p><h4>${copy.cases.contribution}</h4><p>${c.contribution}</p><h4>${copy.cases.work}</h4><p>${c.work}</p><h4>${copy.cases.value}</h4><p>${c.value}</p><h4>${copy.cases.limitations}</h4><p>${c.limitations}</p>` };
    }
}

// Experience: each role now carries the analytical work done in it, supplied by Miguel.
Object.assign(translations.en.experience, {
    dataLabel: 'Data work',
    job1: Object.assign({}, translations.en.experience.job1, {
        desc: 'Leading the Iberia service team, ensuring technical support quality and operational milestones.',
        data: 'Built the automated Tableau dashboards behind EMEA cost per system, install base and average sales price, with Alteryx running the ETL. Also the Salesforce reporting for service KPIs — PM completion, calls per system, callback rate and work-order completion. The ASP analysis is used to reprice maintenance contracts by platform, customer and contract type, and a separate dashboard flags contracts approaching renewal for the sales team.'
    }),
    job2: Object.assign({}, translations.en.experience.job2, {
        data: 'Where the dashboard work started: the first shared service reporting, moving recurring analysis off individual spreadsheets.'
    }),
    job3: Object.assign({}, translations.en.experience.job3, {
        data: 'Built internal applications with Power Apps and Excel to support day-to-day service work.'
    })
});
Object.assign(translations.es.experience, {
    dataLabel: 'Trabajo con datos',
    job1: Object.assign({}, translations.es.experience.job1, {
        desc: 'Lidero el equipo de servicio de Iberia, asegurando la calidad del soporte técnico y los hitos operativos.',
        data: 'Construyo los dashboards automáticos de Tableau de coste por sistema, base instalada y ASP en EMEA, con Alteryx encargándose del ETL. También el reporting en Salesforce de los KPIs de servicio: PM completion, llamadas por sistema, tasa de callback y cierre de órdenes de trabajo. El análisis de ASP se usa para reajustar el precio de los contratos de mantenimiento por plataforma, cliente y tipo de contrato, y otro dashboard avisa al equipo de ventas de los contratos próximos a vencer.'
    }),
    job2: Object.assign({}, translations.es.experience.job2, {
        data: 'Aquí empezó el trabajo de dashboards: el primer reporting de servicio compartido, sacando el análisis recurrente de los excels individuales.'
    }),
    job3: Object.assign({}, translations.es.experience.job3, {
        data: 'Desarrollé aplicaciones internas con Power Apps y Excel para apoyar el día a día del servicio.'
    })
});

// The About thesis: why the two identities reinforce each other rather than compete.
Object.assign(translations.en.about, {synthesis: 'That is the difference. I know which questions the data has to answer before I build anything, and I can tell when a number is wrong because I know what the operation behind it looks like.'});
Object.assign(translations.es.about, {synthesis: 'Esa es la diferencia. Sé qué preguntas tienen que responder los datos antes de construir nada, y detecto cuándo una cifra está mal porque conozco la operación que hay detrás.'});

// Capability bars: tiers reflect the production work described in the timeline.
Object.assign(translations.en.about, {skill1: 'Data integration & ETL — Alteryx', skill2: 'Dashboard design — Tableau, Salesforce', skill3: 'Data quality & record reconciliation', skill4: 'Service & contract analytics — cost, ASP', skill5: 'SQL & Python', skill6: 'Medtech service operations — EMEA scope'});
Object.assign(translations.es.about, {skill1: 'Integración de datos y ETL — Alteryx', skill2: 'Diseño de dashboards — Tableau, Salesforce', skill3: 'Calidad de dato y conciliación de registros', skill4: 'Analítica de servicio y contratos — coste, ASP', skill5: 'SQL y Python', skill6: 'Operaciones de servicio medtech — alcance EMEA'});


// Stats band. Figures are derived from the timeline and project lists on this page.
Object.assign(translations.en, {stats: {tools: 'Data tools in use', cases: 'Analytical cases', apps: 'Applications built', domain: 'Years leading technical service'}});
Object.assign(translations.es, {stats: {tools: 'Herramientas de datos', cases: 'Casos de análisis', apps: 'Aplicaciones construidas', domain: 'Años liderando servicio técnico'}});

// A lightweight interactive data field, paused off-screen and on reduced motion.
document.addEventListener('DOMContentLoaded', () => {
    if (!document.body.classList.contains('editorial')) return;
    const reduced = matchMedia('(prefers-reduced-motion: reduce)');
    const fine = matchMedia('(hover: hover) and (pointer: fine)');
    const field = document.createElement('div');
    field.className = 'data-atmosphere';
    field.setAttribute('aria-hidden', 'true');
    const canvas = document.createElement('canvas');
    field.append(canvas);
    document.body.prepend(field);
    const ctx = canvas.getContext('2d');
    if (!ctx) { field.remove(); return; }
    const aura = document.createElement('div');
    aura.className = 'mouse-aura';
    aura.setAttribute('aria-hidden', 'true');
    document.body.append(aura);

    /* The code veil. Eight snippets of the analytical work the page claims, sitting
       between each band's colour and its text: section content is lifted to z-index
       2 in CSS, so the veil can never cover a word. Every table, column and figure
       here is invented -- the confidentiality rule applies to decoration too. */
    const SNIPPETS = [
`-- one row per asset, latest record wins
with ranked as (
  select *, row_number() over (
    partition by asset_id
    order by loaded_at desc) as rn
  from install_base_raw
)
select * from ranked where rn = 1;`,
`merged = contracts.merge(
    install_base, on="asset_id",
    how="outer", indicator=True, validate="m:1")

gaps = merged.loc[merged._merge != "both"]
log.warning("%d assets disagree", len(gaps))`,
`select platform, country, fiscal_year,
       sum(labour + travel + parts) as cost,
       count(distinct asset_id)     as systems,
       sum(labour + travel + parts)
         / nullif(count(distinct asset_id), 0)
         as cost_per_system
from service_orders
group by 1, 2, 3
having count(distinct asset_id) >= 5;`,
`def parse_date(raw):
    """Source systems never agree on a format."""
    for fmt in ("%Y-%m-%d", "%d/%m/%Y", "%d.%m.%Y"):
        try:
            return datetime.strptime(raw.strip(), fmt)
        except (ValueError, AttributeError):
            continue
    return None`,
`select date_trunc('quarter', ends_on) as quarter,
       count(*)          as contracts,
       sum(annual_value) as exposure
from contracts
where ends_on between current_date
      and current_date + interval '18 months'
group by 1
order by 1;`,
`checks = {
    "missing_cluster": df.cluster.isna().mean(),
    "zero_value_priced":
        (df.value.eq(0) & df.priced).mean(),
    "duplicate_accounts":
        df.account.str.casefold().duplicated().sum(),
}
assert checks["missing_cluster"] < 0.05, checks`,
`select contract_type,
       round(avg(annual_value), 2) as asp,
       percentile_cont(0.5) within group (
         order by annual_value) as median_value
from contracts
where status = 'active'
group by contract_type
order by asp desc;`,
`select ib.cluster,
       count(*) filter (
         where c.asset_id is not null) as covered,
       count(*)                        as total,
       round(100.0 * count(*) filter (
         where c.asset_id is not null)
         / count(*), 1) as pct_covered
from install_base ib
left join contracts c using (asset_id)
group by ib.cluster;`,
    ];
    const veil = document.createElement('div');
    veil.className = 'code-veil';
    veil.setAttribute('aria-hidden', 'true');
    const veilGrid = document.createElement('div');
    veilGrid.className = 'code-veil__grid';
    /* Twice through, so a wide viewport has no empty cells. */
    SNIPPETS.concat(SNIPPETS).forEach(code => {
        const pre = document.createElement('pre');
        pre.textContent = code;
        veilGrid.append(pre);
    });
    veil.append(veilGrid);
    document.body.prepend(veil);
    let veilFrame = 0, veilAt = null;
    /* The ink follows the band under the cursor. A fixed layer spans bands of
       opposite lightness and no single colour reads on both, but only the band the
       cursor is over is ever revealed, so one colour at a time is enough. */
    let veilInk = new WeakMap();
    function inkFor(band) {
        if (!band) return null;
        let ink = veilInk.get(band);
        if (!ink) {
            ink = getComputedStyle(band).getPropertyValue('--primary-color').trim();
            if (ink) veilInk.set(band, ink);
        }
        return ink || null;
    }
    function paintVeil() {
        veilFrame = 0;
        if (!veilAt) return;
        veil.style.setProperty('--vx', veilAt.x + 'px');
        veil.style.setProperty('--vy', veilAt.y + 'px');
        const under = document.elementFromPoint(veilAt.x, veilAt.y);
        const ink = inkFor(under && under.closest('main > section, .footer'));
        if (ink) veil.style.setProperty('--veil-ink', ink);
    }
    const toggle = document.createElement('button');
    toggle.type = 'button';
    toggle.className = 'effects-toggle';
    document.querySelector('.footer__container').append(toggle);
    let paused = false;
    try { paused = localStorage.getItem('site-effects') === 'paused'; } catch (_) {}
    let width = 0, height = 0, frame = 0, lastPaint = 0;
    let pointer = {x:-1000, y:-1000}, ring = {x:0,y:0}, ringFrame = 0;
    let onScreen = true;
    /* Two gates, not one. `interactive` is about the visitor: pointer effects run
       wherever they are on the page. `enabled` adds the canvas's own condition, that
       its field is still on screen. Folding the two together in TASK-012 killed the
       cursor ring below the hero as a side effect. */
    const interactive = () => !paused && !reduced.matches && fine.matches && width > 768 && !document.hidden;
    const enabled = () => interactive() && onScreen;
    function updateLabel() {
        const es = document.documentElement.lang === 'es';
        toggle.textContent = reduced.matches ? (es ? 'Movimiento reducido' : 'Reduced motion') : paused ? (es ? 'Activar efectos' : 'Enable effects') : (es ? 'Pausar efectos' : 'Pause effects');
        toggle.setAttribute('aria-pressed', String(paused || reduced.matches));
        toggle.disabled = reduced.matches;
    }
    function draw(time = 0) {
        ctx.clearRect(0,0,width,height);
        const light = document.documentElement.classList.contains('light-theme');
        const rgb = light ? '35,93,71' : '181,221,193';
        const points = [];
        for (let row=0; row<5; row++) for (let col=0; col<9; col++) {
            const i=row*9+col;
            let x=width*(col+.35)/8, y=height*(row+.4)/5;
            x += Math.sin(time*.00012+i*.8)*20;
            y += Math.cos(time*.0001+i*.6)*28;
            const dx=x-pointer.x, dy=y-pointer.y, distance=Math.hypot(dx,dy);
            if(enabled() && distance<160 && distance>0) {x+=dx/distance*(160-distance)*.12; y+=dy/distance*(160-distance)*.12;}
            points.push({x,y});
        }
        ctx.lineWidth=.7;
        for(let i=0;i<points.length;i++) {
            const p=points[i];
            for(const j of [i+1,i+9]) {
                if(j>=points.length || (j===i+1 && i%9===8)) continue;
                const q=points[j];
                ctx.strokeStyle=`rgba(${rgb},${light ? .085 : .11})`;
                ctx.beginPath();ctx.moveTo(p.x,p.y);ctx.lineTo(q.x,q.y);ctx.stroke();
            }
            const near=enabled() && Math.hypot(p.x-pointer.x,p.y-pointer.y)<150;
            ctx.fillStyle=`rgba(${rgb},${near ? .5 : .17})`;
            ctx.beginPath();ctx.arc(p.x,p.y,near?2.4:1.4,0,Math.PI*2);ctx.fill();
        }
    }
    function loop(time) {
        frame=0;
        if(!enabled()) return;
        if(time-lastPaint>40) {draw(time);lastPaint=time;}
        frame=requestAnimationFrame(loop);
    }
    function sync() {
        cancelAnimationFrame(frame);frame=0;
        cancelAnimationFrame(ringFrame);ringFrame=0;
        aura.classList.remove('is-visible');
        veil.classList.remove('is-lit');
        veil.style.removeProperty('--veil-ink');
        veilInk = new WeakMap();  /* the theme may have flipped; cached inks are stale */
        veilAt = null;
        pointer={x:-1000,y:-1000};
        field.style.removeProperty('--field-x');field.style.removeProperty('--field-y');
        updateLabel();draw();
        if(enabled()) frame=requestAnimationFrame(loop);
    }
    function resize() {
        width=innerWidth;height=innerHeight;
        const ratio=Math.min(devicePixelRatio||1,1.5);
        canvas.width=Math.round(width*ratio);canvas.height=Math.round(height*ratio);
        ctx.setTransform(ratio,0,0,ratio,0,0);sync();
    }
    function moveRing() {
        ringFrame=0;
        if(!interactive()) return;
        ring.x+=(pointer.x-ring.x)*.25;ring.y+=(pointer.y-ring.y)*.25;
        aura.style.transform=`translate3d(${ring.x}px,${ring.y}px,0)`;
        if(Math.abs(ring.x-pointer.x)+Math.abs(ring.y-pointer.y)>.5) ringFrame=requestAnimationFrame(moveRing);
    }
    document.addEventListener('pointermove', event => {
        if(!interactive() || event.pointerType!=='mouse') return;
        if(event.target.closest('input,textarea,select,[contenteditable="true"]')) {aura.classList.remove('is-visible');return;}
        if(!aura.classList.contains('is-visible')) ring={x:event.clientX,y:event.clientY};
        const box=field.getBoundingClientRect();
        pointer={x:event.clientX-box.left,y:event.clientY-box.top};
        field.style.setProperty('--field-x',`${100*pointer.x/width}%`);
        field.style.setProperty('--field-y',`${100*pointer.y/height}%`);
        aura.classList.add('is-visible');
        aura.classList.toggle('is-interactive',Boolean(event.target.closest('a,button,summary,[role="button"]')));
        veilAt = {x: event.clientX, y: event.clientY};
        veil.classList.add('is-lit');
        if(!veilFrame) veilFrame = requestAnimationFrame(paintVeil);
        if(!ringFrame) ringFrame=requestAnimationFrame(moveRing);
    }, {passive:true});
    document.documentElement.addEventListener('pointerleave',()=>{aura.classList.remove('is-visible');veil.classList.remove('is-lit');veilAt=null;pointer={x:-1000,y:-1000};cancelAnimationFrame(ringFrame);ringFrame=0;cancelAnimationFrame(veilFrame);veilFrame=0;});
    toggle.addEventListener('click',()=>{paused=!paused;try{localStorage.setItem('site-effects',paused?'paused':'on');}catch(_){}sync();});
    if('IntersectionObserver' in window) {
        new IntersectionObserver(entries => {
            onScreen = entries[0].isIntersecting;
            sync();
        }, {rootMargin:'120px'}).observe(field);
    }
    addEventListener('resize',resize,{passive:true});
    document.addEventListener('visibilitychange',sync);
    reduced.addEventListener('change',sync);fine.addEventListener('change',sync);
    new MutationObserver(sync).observe(document.documentElement,{attributes:true,attributeFilter:['class','lang']});
    resize();
});

/* The project cards are role="button" and open a modal on click. The source-code
   link sits inside one, so without this a click would open the repo in a new tab
   *and* the modal behind it. Capture phase, so it runs before the card's own
   handler whatever order the scripts loaded in. */
document.addEventListener('click', function (event) {
    var code = event.target.closest && event.target.closest('.project-card__code');
    if (code) event.stopPropagation();
}, true);

/* Same for the keyboard: the card responds to Enter, and so should the link,
   but only the link. */
document.addEventListener('keydown', function (event) {
    if (event.key !== 'Enter' && event.key !== ' ') return;
    var code = event.target.closest && event.target.closest('.project-card__code');
    if (code) event.stopPropagation();
}, true);
